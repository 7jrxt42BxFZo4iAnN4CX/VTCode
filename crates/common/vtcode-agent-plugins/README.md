# vtcode-agent-plugins

Agent Plugins 1.0.0 loader for VT Code. Portable plugin format: a directory with a root `plugin.json` manifest, optional `skills/*/SKILL.md` Agent Skills, and optional `mcp.json` MCP server configuration.
